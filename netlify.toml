[build]
  command   = "pnpm --filter @workspace/tournament run build"
  publish   = "artifacts/tournament/dist/public"

[build.environment]
  NODE_VERSION = "22"
  PORT         = "3000"
  BASE_PATH    = "/"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
